services:
  frontend:
    image: ${FRONTEND_IMAGE}
    container_name: finote-frontend-prod

    ports:
      - "8082:8080"

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:8080"]
      interval: 30s
      timeout: 5s
      retries: 3