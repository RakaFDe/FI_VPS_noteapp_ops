apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finote-backend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: apifinote.rakafitrap.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: finote-backend
            port:
              number: 80
